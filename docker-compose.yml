services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: woodnest
    ports:
      - "5432:5432"
    volumes:
      - woodnest_pgdata:/var/lib/postgresql/data

volumes:
  woodnest_pgdata:

